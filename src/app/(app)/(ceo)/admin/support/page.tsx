import { SupportTicketsBoard } from "@/components/sunland/support-tickets-board";

export const metadata = {
  title: "Support Tickets - Sunland ERP",
  description: "Cross-department ERP support tickets queue and management.",
};

export default function SupportTicketsPage() {
  return <SupportTicketsBoard />;
}
